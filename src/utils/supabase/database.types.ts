// Placeholder until the dev Supabase project has a real schema (Phase 3 adds
// `coffee_shops`). Regenerate for real via `npm run gen:types` once
// NEXT_PUBLIC_SUPABASE_URL points at a live project — this file gets
// overwritten wholesale at that point, so nothing here is meant to last.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
